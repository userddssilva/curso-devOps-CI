name: Um pipeline para meu ello world
on: push

jobs:
  build:
    name: Hello World Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ../action-a/action.ymal
        with:
            MY_NAME: "Dayvson Silva"